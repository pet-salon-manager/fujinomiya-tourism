# 富士宮たびナビ
静岡県富士宮市に特化した観光案内PWAの試作版です。

## 入っている機能
- 観光地・自然・体験・グルメ・学び・登山のカテゴリ検索
- キーワード検索
- お気に入り保存（端末内）
- Google Mapsで地図表示
- 現在地から目的地へのルート起動
- モデルコース
- ホーム画面追加に対応するPWA構成

## 起動
ローカルではHTTPサーバー経由で開いてください。
例: `python3 -m http.server 8000`
その後 `http://localhost:8000` を開きます。

## 公開前に追加推奨
- 公式情報で住所・営業時間・料金・定休日を検証
- 実写写真
- 飲食店・宿泊施設・温泉・キャンプ場データの拡充
- 現在地からの距離順ソート
- 富士宮市公式観光情報への導線
- 管理画面・クラウドDB


## v2 更新
- 富士宮らしい写真を背景に使用
- 富士宮やきそばを中心に飲食店20件を追加
- 店舗の住所・電話番号をカードに表示
- 「富士宮やきそば」「飲食店」カテゴリを追加


## v3 更新
- 固定登録スポットを 72 件まで拡張
- 宿泊・温泉・キャンプ・公園・神社史跡・交通などカテゴリを大幅追加
- 富士宮市内をカテゴリ別にさらに探せる検索カード12種類を追加
- 登録件数表示を追加


## v4 写真対応
- 全施設カードの最上部に大きな写真枠を追加
- Wikimedia Commonsから施設名・地域名で実写写真を自動取得
- 写真元リンクを表示
- 見つからない場合は富士宮の実写イメージ写真を表示


## v5 説明文・画像改善
- 72施設の説明文をより詳しく更新
- 関係のない写真が出ないよう、自動写真検索を停止
- 実写が未確認の施設は説明文に沿った実写風イメージ画像に変更
- 各カード上部の大きな画像表示は維持


## v6 飲食店大量検索
- 富士宮市内の飲食店を数百店舗規模で探せる検索セクションを追加
- 35ジャンルの飲食店検索に対応
- 富士宮やきそば、定食、和食、寿司、ラーメン、焼肉、カフェ、スイーツなど
- 固定登録外の実在店舗もGoogle Maps検索から探せる構成に変更


## v7 営業時間なし固定登録
- 飲食店の営業時間表示を廃止
- これまで確認できた実在飲食店 35店舗を店名・住所・電話番号付きで固定登録
- 実写写真が未確認の店舗は実写風イメージを表示
- 数百店舗化に向けた固定登録方式へ移行

## v8 100店舗化準備
- 現在、実在確認済みの固定登録飲食店は 36 店舗
- 100店舗まで残り 64 店舗
- `restaurants_to_100.csv` に残り店舗を追加すれば、そのまま固定データ化できる構成
- 営業時間は登録しない方針を維持
- 写真が未確認の店舗は実写風イメージを使う方針を維持

## v9 添付データから100店舗固定登録
- 添付テキストの飲食店情報を抽出して既存データと統合
- 飲食店を100店舗まで固定登録
- 営業時間は登録・表示しない
- 店名、住所、電話番号、特徴説明を保存
- 添付テキストには写真が含まれていないため、関係ない写真を避けるため実写風イメージを表示
- 追加店舗数を含む固定飲食店総数: 100

## v10 ホーム直表示
- 「一覧を見る」「店舗を探す」などの外部検索導線を廃止
- 観光地・施設・飲食店100店舗をホーム画面に直接一覧表示
- 全固定データ 148 件をホームで表示可能
- 検索欄とカテゴリはホーム内の一覧を絞り込む用途だけに変更
- 各カードの地図・現在地から案内は維持
- ミント系背景・大きめカード・横スクロールカテゴリで見た目を整理

## v11 ホーム一覧表示修正
- ホームのカード一覧が消えていた不具合を修正
- 全148件をホーム画面に直接表示
- 飲食店100店舗も各店舗カードとして表示
- 「一覧を見る」「店舗を探す」の外部検索ブロックは使わない
- iPhoneの古いキャッシュを避けるためService Workerをv11に更新

## v12 完全修正版
- JavaScriptの構文エラーを修正
- 重複していた cards / spotCount IDを削除
- モデルコースをホーム一覧の前に出さない構成へ変更
- 全148件をホームに直接カード表示
- spots.jsonを no-store で読み込み、古いキャッシュを回避
- Service Workerをv12へ刷新

## v13 カード表示変更
- カード上の写真・実写風イメージを完全に非表示
- 各カードに「ホームページ」ボタンを追加
- 「店舗を検索」ボタンをホームページとは別に追加
- 「現在地から案内」ボタンは維持
- homepage URL が固定登録されていない施設は「施設名 + 富士宮 + 公式ホームページ」で検索する仕様


## v14 ホームアイコン更新
- iPhoneホーム画面用アイコンを富士宮市らしい綺麗な写真イメージに変更
- 富士山・赤鳥居・神社の風景を使った新アイコンを追加
- manifest.webmanifest に 192px / 512px アイコンを設定
- apple-touch-icon も追加

## v15 修正
- 「すべて」以外のカテゴリボタンが押せない不具合を修正
- カテゴリボタンを data-cat + addEventListener 方式に変更
- 「店舗を検索」ボタンを「検索」に変更
- 富士山本宮浅間大社の「ホームページ」ボタンを http://www.fuji-hongu.or.jp/sengen/ に直接接続
- iPhoneホームアイコンをv15ファイル名へ変更しキャッシュ更新
- manifestとapple-touch-iconをv15へ更新

## v16 公式ホームページ直リンク
- 静岡県富士山世界遺産センター → https://mtfuji-whc.jp/
- 白糸ノ滝 → https://www.city.fujinomiya.lg.jp/1015150000/p001607.html
- 湧玉池 → https://www.pref.shizuoka.jp/kankosports/bunkageijutsu/bunkazai/1002825/1041003/1041889/1004987/1021566.html

## v17 管理者編集
- 管理者ログインを追加
- 初回ログイン時に管理者PINを設定
- 店舗だけでなく観光地・公園・温泉・宿泊・登山スポットなど「場所」も同じ画面から追加・修正
- 店名/場所名、カテゴリ、エリア、住所、電話、ホームページ、説明文を編集可能
- カードに管理者用「編集」ボタンを表示
- 新規追加・削除・JSON書き出し/読み込みに対応
- このGitHub Pages版では編集は端末ローカル保存。全利用者への共通反映にはクラウドDBが必要

## v18 SupabaseクラウドDB接続
- Project URL: https://tgyaelnwcjuyhvkjjqov.supabase.co
- Publishable keyをブラウザアプリに設定
- 一覧データはSupabase public.spotsから直接読み込み
- Supabase障害時のみspots.jsonを予備データとして使用
- 管理者ログインはSupabase Authのメールアドレス/パスワード
- 管理者の追加・修正・削除はSupabaseへ直接保存
- 全利用者へ変更内容を共通反映
- `fujinomiya_supabase_admin_policies.sql` をSupabase SQL Editorで実行して書き込み権限を有効化してください

## v19 JWT自動更新修正
- JWT expired / PGRST303 が出た場合、refresh tokenで自動更新して保存・削除を1回再試行
- refresh tokenをlocalStorageにも保持し、PWA再起動後でも管理者セッションを復元しやすくした
- 管理者ボタン押下時に有効なrefresh tokenがあれば再ログイン入力なしで管理者モードへ復帰
- 更新に失敗した場合のみログアウト→再ログインを案内

## v20 管理者写真アップロード
- 管理者編集画面に写真ファイル選択を追加
- Supabase Storage `spot-images` へ写真をアップロード
- アップロード後の公開URLを `spots.image` に保存
- 既存施設の登録写真を管理画面でプレビュー
- 写真を外す操作にも対応
- カード上の写真表示は従来どおり行わない（管理データとして保存）

## v21 カード写真表示
- `spots.image` に写真URLが登録されている施設・店舗はカード上部に大きく写真表示
- 写真未登録の施設は従来どおり文字中心カード
- 画像読み込みに失敗した場合は写真枠を自動で非表示
- Supabase Storage の公開URLをそのまま利用

## v22 写真編集UI修正
- 管理者編集画面に写真ファイル選択を確実に表示
- 写真URL入力、プレビュー、アップロード、写真を外す機能を追加
- 既存施設を編集すると登録済み写真をプレビュー
- 保存時に `spots.image` / `imageType` へ反映
- Supabase Storage `spot-images` へアップロード

## v23 5件表示・ホームアイコン刷新
- 施設・店舗・場所カードを1ページ5件ずつ表示
- 前へ / 次へボタンとページ番号を追加
- 検索やカテゴリ変更時は1ページ目へ戻る
- iPhoneホームアイコンを富士山・浅間大社・滝・桜をモチーフにした富士宮らしい画像へ変更
- apple-touch-icon と manifest のアイコンを新画像へ統一

## v24 iPhoneホームアイコン強制更新
- iOS向けに180x180の `apple-touch-icon-v24.png` をルート直下に配置
- `apple-touch-icon-precomposed` も追加
- manifestアイコンをバージョン付きの別ファイル名へ変更
- manifestのstart_url/id、Service Worker revisionをv24へ更新
- iPhoneの「ホーム画面に追加」プレビューが旧アイコンを掴みにくい構成へ修正

## v25 5件ページング修正
- 一覧を必ず1ページ最大5件に制限
- 前へ / 次へボタンを実装
- ページ番号と総ページ数を表示
- 検索・カテゴリ変更時は1ページ目に戻る
- ページ移動時はカード一覧上部へ自動スクロール

## v26 観光スポット拡充
- ユーザー提供の観光スポット情報を 92 件追加/更新
- Supabase用upsert SQLを同梱
- ローカル予備データ `spots.json` にも反映
- 入力内容はユーザー提供情報をそのまま基礎データとして使用（外部検証は未実施）

## v27 Supabase最新228件の反映修正
- Supabase REST取得を `cache: no-store` + 毎回異なるクエリ付きに変更し、古い148件キャッシュを回避
- 読み込んだ件数を画面に「Supabase最新データ：○件読み込み済み」と表示
- 詳細カテゴリを既存カテゴリボタンにグループ化
  - 世界遺産・社寺 / 展望・その他名所 → 観光
  - 自然名所・滝・湖沼 → 自然
  - 高原・牧場・レジャー / 公園・スポーツ → 体験
  - 文化・博物館・施設 → 学ぶ
  - 温泉・リフレッシュ → 温泉
  - グルメ・特産品 → グルメ
- 5件ずつページ送りは維持

## v29 ローカル固定背景版
- 背景画像を `assets/fujinomiya-background.png` としてZIP内に同梱
- 外部画像URLに依存しないため、GitHub Pagesでも背景が消えません
- 背景は画面全体に固定し、スクロールしても動きません
- Supabase最新データ取得・5件ずつページ表示・管理者写真アップロード機能は維持
- Service Worker / manifest / spots.json に v29 のキャッシュ更新を適用


## v30 Supabase接続・固定背景の同時修正
- Supabase REST取得URLを `/rest/v1/spots?select=*&order=id.asc` に戻し、誤った `&_=` パラメータを削除
- `fetch` の `cache: no-store` は維持して最新データを取得
- 背景画像 `assets/fujinomiya-background.png` をZIP内に実ファイルとして保持
- iPhoneで背景が消える原因だった負のz-indexを廃止し、固定背景を前面レイヤーとして安全に表示
- 背景はスクロールしても動かない固定表示
- Service Worker / manifest / fallback data のrevをv30へ更新

## v31 iPhone背景表示修正
- iOS/PWAで消えることがある `body::before` の固定背景を廃止
- ZIP内の `assets/fujinomiya-background.png` を実際の `<img>` 要素として固定表示
- 背景画像は `position: fixed` + `object-fit: cover`
- アプリ本体を背景より前面に固定
- Service Workerをv31に更新し、背景画像もno-cache対象に追加
- Supabase接続修正（正しいREST URL）はv30のまま維持

## v32 背景画像の完全埋め込み修正
- 背景写真を `index.html` 内に直接埋め込み（Data URI）
- GitHub上で `assets` フォルダのパスやアップロード漏れがあっても背景が消えない方式
- 背景は `position: fixed` で画面全体に固定
- 同じ写真を `assets/fujinomiya-background-v32.jpg` にも予備保存
- iPhone/PWAで壊れた画像アイコンが出ない構成に変更
- Supabase接続処理はv30/v31の正しいREST URL修正を維持

## v33 交通カード廃止・駅/バス停マップ
- カード一覧・施設情報からカテゴリ「交通」を非表示
- 管理者カテゴリ選択肢から「交通」を削除
- ホーム画面に「駅・バス停マップ」を追加
- OpenStreetMap / Overpass APIから富士宮市行政区域内の鉄道駅・バス停を取得して地図表示
- 「すべて / 鉄道駅 / バス停」切替ボタンを追加
- 駅・バス停マーカーをタップすると名称を表示
- 取得件数を画面に表示
- 地図上の交通データの網羅性はOpenStreetMapの登録状況に依存

## v34 駅・バス停マップ表示修正
- OpenStreetMapタイルURLを `tile.openstreetmap.org` 単一ホストへ変更
- iPhone/PWAで起きる地図サイズ計算不具合対策として `invalidateSize()` を追加
- 地図表示範囲を富士宮市周辺へ固定し、遠方へズームアウトしすぎる問題を修正
- 明らかに富士宮市外の誤座標をマーカー表示から除外
- タイル読み込みエラー時にレイアウト再計算
- 駅・バス停件数表示を「富士宮市内」に変更

## v35 地図タイル・マーカー表示の再修正
- 背景地図をOpenStreetMap直タイルからEsri World Street Mapへ変更
- iPhoneで欠けていた地図タイルの読み込み安定性を改善
- 駅・バス停マーカーを画像/DivIcon方式からSVGベースのcircleMarkerへ変更
- 鉄道駅は緑、バス停はオレンジの丸で表示
- マーカータップで名称を表示
- Leafletの各paneの重なり順を明示してマーカーが地図の下に隠れないよう修正

## v36 地図方式を全面変更
- Leafletのタイル描画を廃止
- 背景地図をOpenStreetMap公式埋め込み地図（iframe）へ変更
- 鉄道駅・バス停の位置はHTMLオーバーレイで固定表示
- iPhone/PWAで地図タイルが欠ける問題を回避
- 駅は緑、バス停はオレンジの丸
- マーカーをタップすると名称と「地図アプリで見る」を表示
- 「すべて / 鉄道駅 / バス停」の切替機能を維持

## v37 駅・バス停マップ位置ずれ修正
- 駅・バス停マーカーの緯度投影を単純比例からWeb Mercatorへ変更
- OpenStreetMap埋め込み地図とマーカーの座標系を一致
- 埋め込み地図のパン・ズームを無効化し、背景地図とマーカーがずれないよう固定
- 「大きな地図で見る」ボタンを追加
- バス停マーカーを少し小さくして密集時の見やすさを改善

## v38 インタラクティブ地図
- 地図に＋／−ボタンを追加し、拡大・縮小可能
- 「現在地」ボタンを追加し、iPhoneの位置情報を青いマーカーで表示
- 観光・自然・体験・グルメ等のカテゴリを選ぶと、そのカテゴリの施設・場所だけを青マーカーで表示
- 「すべて」は全カテゴリ対象
- 鉄道駅（緑）・バス停（オレンジ）はカテゴリに関係なく常時表示
- 施設位置は住所/施設名をOpenStreetMap Nominatimで順次位置取得し、端末にキャッシュ
- カテゴリ地図は1回最大30施設を表示し、iPhoneでの負荷を抑制
